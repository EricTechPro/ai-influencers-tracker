# Build handoff: fill this in, then walk away

Companion to [2026-07-27-data-spine.md](2026-07-27-data-spine.md). That file is the plan; this
file is the input sheet. Everything Claude needs from you is on this one page. Edit the
checkboxes and blanks, save, then say **"handoff filled, go"** and the build runs unattended.

Anything left unticked uses the stated default.

---

## 1. Credentials (one key left)

Done 2026-07-28: `YOUTUBE_API_KEY`, `VIDIQ_API_KEY`, `MCP_VIDIQ_URL` and `FIRECRAWL_API_KEY`
were copied from `EricOS/.env` into the project `.env` and **live-tested**: YouTube returned a
real `channels.list` item, vidIQ reported 1,136 credits (resets 2026-08-04), Firecrawl auth
passed. No key value ever entered the transcript.

- [x] `GITHUB_TOKEN` pasted into `.env` and live-verified 2026-07-28: authenticated at
  5,000 req/hr core and the 30/min search window.

**Section 1 is complete. All four keys tested working against the real APIs.**

## 2. The roster (blocking for real data, not for code)

`config/channels.json` exists but has no `channel_id` and no `niche` columns. All code is built
test-first against fixtures, so the build proceeds regardless; only the **real** sweeps need this.

`channel_id` resolution is mechanical (one API call per handle). Pick one:

- [x] **Default: script writes `channel_id` directly into `config/channels.json`.** A timestamped
  backup copy lands in `_raw/` first, the script touches the `channel_id` field and nothing
  else, and this is the one-time resolution pass system.md T1 describes. Fastest path to real data.
- [ ] Conservative: script writes `_raw/roster_candidates.json` only, and I merge by hand when
  I am back. (Real sweeps, the backfill, and the leaderboard wait for me.)

`niche` is judgment, not mechanics, so it stays yours either way:

- [ ] I will fill `niche` on the roster rows myself, later. Until then the niche filter shows
  "all" and everything else works.

## 3. The topic tree

`config/topics.json` has 24 drafted leaves grounded in 1,889 real watchlist titles. The plan
says you groom them (cut, rename, merge, fix shape tags).

- [x] **Default: ship the draft as-is.** `coverage_rate` in `meta.json` will show how well it
  fits, and I prune with real numbers instead of guesses.
- [ ] Wait: I want to groom the tree first. (Blocks topic matching and the opportunity table.)

## 4. Money and machine access (pre-authorizations)

These are the steps a background run would otherwise have to stop and ask about. Ticked = go.

| # | Action | Cost | Authorized? |
|---|---|---|---|
| 4a | vidIQ `channel_stats` backfill, all 72 channels | **360 credits, once** (balance ~1,141 → ~781) | [ ] yes |
| 4b | First weekly keyword sweep, one call per leaf | ~120 credits (5 × 24 leaves) | [ ] yes |
| 4c | One-time comment backfill, ~3,600 videos | ~3,600 YouTube units, **free**, fits in one day | [x] yes |
| 4d | Install the launchd agent (writes one plist to `~/Library/LaunchAgents/`, runs the sweep daily at 09:00) | free | [ ] yes |
| 4e | First real daily sweep (channels, uploads, video counts, GitHub, trending) | ~250 YouTube units, free | [x] yes |

Notes:
- 4a is what makes growth windows live on day one instead of day 91. Without it every growth
  cell reads `building, N of 90` for three months. The cost guard enforces a 200-credit reserve
  and a 400-credit per-run ceiling regardless of what is ticked here.
- 4b is what feeds the demand axis. Without it, verdicts run on repo velocity alone and every
  score renders `x / 75` with `keyword_volume: no data, weight excluded` (honest, just thinner).
- If 4a or 4b is unticked, the build still completes; those steps are skipped and logged, and
  you can run them later with one command each (written into the final report).

## 5. Execution style

- [x] **Default (per Eric, 2026-07-28): fresh context window, subagent-driven** via
  `superpowers:subagent-driven-development`, one subagent per task, committing task by task.
- [ ] Inline in one session via `superpowers:executing-plans`.

Kickoff prompt for the fresh window, verbatim:

> Execute docs/plans/2026-07-27-data-spine.md with superpowers:subagent-driven-development.
> Read docs/plans/2026-07-28-handoff.md first and honor its ticks: skip nothing that is
> authorized, skip and log anything that is not. Do not stop to ask about anything the
> handoff already answers. Task 4 step 6 items that remain unticked get logged, not asked.

The project `.claude/settings.json` allowlist was widened on 2026-07-28 (pytest, ruff, python3,
mkdir, rtk git add/commit, launchctl) so the run does not stall on permission prompts.

## 6. What you get back

When the run ends (or you come back and ask), the report contains, with real command output:

1. `pytest -q` and `ruff check` results, full suite.
2. The quota ledger (`~N of 10,000 units`) and, if 4a/4b ran, the vidIQ ledger
   (balance before − after vs. the previewed bill).
3. The top of the real opportunity table, and `meta.json`'s health block.
4. Anything skipped, and the exact command to run it later.

---

*Sections 1 is the only hard blocker. 2 through 5 have safe defaults already ticked where
free, unticked where they spend credits or touch files outside the repo: 4a, 4b and 4d need
your explicit tick.*
